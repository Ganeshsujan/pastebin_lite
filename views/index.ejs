<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pastebin Lite</title>
    <style>
        :root{
            --bg-1: #667eea;
            --bg-2: #764ba2;
            --card-bg: #ffffff;
            --text-primary: #0f172a;
            --text-secondary: #64748b;
            --border: #e2e8f0;
            --accent: linear-gradient(135deg,var(--bg-1),var(--bg-2));
        }

        *{box-sizing:border-box}

        html,body{height:100%}

        body{
            margin:0;
            font-family: Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;
            background: radial-gradient(1200px 600px at 0% 10%, rgba(102,126,234,0.12), transparent),
                        radial-gradient(900px 500px at 100% 90%, rgba(118,75,162,0.12), transparent),
                        linear-gradient(135deg,var(--bg-1),var(--bg-2));
            -webkit-font-smoothing:antialiased;
            -moz-osx-font-smoothing:grayscale;
            display:flex;
            justify-content:center;
            align-items:center;
            min-height:100vh;
            padding:20px;
        }

        .container{
            background:var(--card-bg);
            border-radius:12px;
            box-shadow:0 10px 30px rgba(2,6,23,0.2);
            padding:32px;
            max-width:640px;
            width:100%;
        }

        h1{
            color:var(--text-primary);
            margin:0 0 6px 0;
            font-size:26px;
            font-weight:700;
            text-transform: uppercase;
        }

        .subtitle{
            color:var(--text-secondary);
            margin:0 0 28px 0;
            font-size:14px;
        }

        .form-group{
            margin-bottom:22px;
        }

        label{
            display:block;
            margin-bottom:8px;
            color:var(--text-primary);
            font-weight:600;
            font-size:14px;
        }

        textarea{
            width:100%;
            min-height:120px;
            padding:12px;
            border:1px solid var(--border);
            border-radius:8px;
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, 'Roboto Mono', 'Courier New', monospace;
            font-size:14px;
            resize:vertical;
            transition:all 0.15s;
            color:var(--text-primary);
            background:#f8fafc;
        }

        textarea:focus{
            outline:none;
            border-color:var(--bg-1);
            background:#fff;
            box-shadow:0 0 0 3px rgba(102,126,234,0.08);
        }

        .options{
            display:grid;
            grid-template-columns:1fr 1fr;
            gap:16px;
        }

        input[type="number"]{
            width:100%;
            padding:10px 12px;
            border:1px solid var(--border);
            border-radius:8px;
            font-size:14px;
            transition:all 0.15s;
            color:var(--text-primary);
            background:#f8fafc;
        }

        input[type="number"]:focus{
            outline:none;
            border-color:var(--bg-1);
            background:#fff;
            box-shadow:0 0 0 3px rgba(102,126,234,0.08);
        }

        .button-group{
            display:flex;
            gap:12px;
            margin-top:28px;
        }

        button{
            flex:1;
            padding:11px 20px;
            border:none;
            border-radius:8px;
            font-size:15px;
            font-weight:600;
            cursor:pointer;
            transition:all 0.15s;
        }

        .btn-submit{
            background:var(--accent);
            color:white;
            box-shadow:0 6px 18px rgba(102,126,234,0.18);
        }

        .btn-submit:hover{
            transform:translateY(-2px);
            box-shadow:0 10px 24px rgba(102,126,234,0.25);
        }

        .btn-submit:active{
            transform:translateY(0);
        }

        .btn-reset{
            background:#f1f5f9;
            color:var(--text-primary);
            border:1px solid var(--border);
        }

        .btn-reset:hover{
            background:#e2e8f0;
            border-color:#cbd5e1;
        }

        .error{
            background:#fef2f2;
            border:1px solid #fecaca;
            color:#991b1b;
            padding:12px;
            border-radius:8px;
            margin-bottom:20px;
            font-size:14px;
        }

        .help-text{
            color:var(--text-secondary);
            font-size:12px;
            margin-top:4px;
        }

        @media (max-width:600px){
            .container{padding:24px}
            h1{font-size:24px}
            .options{grid-template-columns:1fr}
            .button-group{gap:10px}
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Pastebin Lite</h1>
        <p class="subtitle">Share your text instantly with optional expiry</p>
        
        <% if (error) { %>
            <div class="error"><%= error %></div>
        <% } %>
        
        <form method="POST" action="/create">
            <div class="form-group">
                <label for="content">Paste Content *</label>
                <textarea 
                    id="content" 
                    name="content" 
                    required 
                    placeholder="Enter your text here..."></textarea>
                <p class="help-text">Required. Plain text only.</p>
            </div>
            
            <div class="form-group">
                <div class="options">
                    <div>
                        <label for="ttl_seconds">TTL (seconds)</label>
                        <input 
                            type="number" 
                            id="ttl_seconds" 
                            name="ttl_seconds" 
                            min="1" 
                            placeholder="e.g., 3600">
                        <p class="help-text">Optional. Auto-delete after</p>
                    </div>
                    <div>
                        <label for="max_views">Max Views</label>
                        <input 
                            type="number" 
                            id="max_views" 
                            name="max_views" 
                            min="1" 
                            placeholder="e.g., 5">
                        <p class="help-text">Optional. Auto-delete after N views</p>
                    </div>
                </div>
            </div>
            
            <div class="button-group">
                <button type="submit" class="btn-submit">Create Paste</button>
                <button type="reset" class="btn-reset">Clear</button>
            </div>
        </form>
    </div>
</body>
</html>
